---
interface Props {
	data: Record<string, unknown> | Record<string, unknown>[] | Record<string, unknown>[];
}
const { data } = Astro.props;
const escaped = JSON.stringify(data).replace(/</g, '\\u003c');
---
<script type="application/ld+json" is:inline set:html={escaped} />
